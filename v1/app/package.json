{
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "scripts": {
    "web:run": "flutter run -d chrome --web-renderer html",
    "web:build": "flutter build web --web-renderer html",
    "web:b:d": "flutter build web --web-renderer html && firebase deploy",
    "web:bc:d": "flutter build web && firebase deploy"
  }
}
